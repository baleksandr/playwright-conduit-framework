name: Playwright Tests
on:
  push:
    branches: [ main, master ]
  pull_request:
    branches: [ main, master ]

jobs:
  test:
    timeout-minutes: 60
    runs-on: ubuntu-latest
    
    permissions:
      contents: write
      pages: write
      id-token: write

    steps:
    - uses: actions/checkout@v4

    - uses: actions/setup-node@v4
      with:
        node-version: 20

    - name: Install dependencies
      run: npm install

    - name: Install Playwright Browsers
      run: npx playwright install --with-deps

    # 1. Завантажуємо стару історію з гілки gh-pages
    - name: Load Allure history
      uses: actions/checkout@v4
      if: always()
      continue-on-error: true
      with:
        ref: gh-pages
        path: gh-pages-dir

    - name: Copy history to results
      if: always()
      continue-on-error: true
      run: |
        mkdir -p allure-results/history
        cp -R gh-pages-dir/history/* allure-results/history/ || true

    # 2. ЗАПУСК ТЕСТІВ (Чесний: впаде - буде червоним!)
    - name: Run Playwright tests
      env:
        USER_EMAIL: ${{ secrets.USER_EMAIL }}
        USER_PASSWORD: ${{ secrets.USER_PASSWORD }}
      # Тут БЕЗ || true і БЕЗ continue-on-error
      run: npx playwright test
      
    - name: Clean old report
      run: rm -rf allure-report

    # 3. ГЕНЕРАЦІЯ ЗВІТУ (Тільки Allure 3.0 синтаксис)
    - name: Generate Allure Report
      if: always() 
      run: npx allure generate allure-results -o allure-report

    # 4. ДЕПЛОЙ ЗВІТУ (Працює завжди)
    - name: Deploy Allure Report to GitHub Pages
      if: always()
      uses: peaceiris/actions-gh-pages@v3
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./allure-report
        publish_branch: gh-pages

    # 5. ЗБЕРІГАЄМО АРТЕФАКТИ (Скріншоти та Трейси)
    - name: Upload Playwright Artifacts
      if: always()
      uses: actions/upload-artifact@v4
      with:
        name: playwright-report
        path: playwright-report/
        retention-days: 30
